// examples/demo.ltl — runnable demo for ltl_chip8_emu

import std::io
use ltl_chip8_emu as lib

fn main() {
    io::println("ltl_chip8_emu demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}